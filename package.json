{
  "name": "modal-factory",
  "private": true,
  "workspaces": [
    "packages/*",
    "packages/adapters/*",
    "example"
  ],
  "scripts": {
    "build": "npm run build --workspace=@modal-factory/core && npm run build --workspace=@modal-factory/adapters-redux && npm run build --workspace=@modal-factory/adapters-tanstack-store",
    "dev": "npm run dev --workspace=examplev2"
  }
}
